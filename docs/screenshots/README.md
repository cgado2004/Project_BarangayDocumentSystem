# Screenshots to capture

For submission. Run the app (startup project = **BarangayDocumentSystem.UI**).

## Required
1. **Dashboard** — the 10 stat cards and both breakdown lists
2. **Residents** — grid with the sidebar visible, showing the themed header row
3. **Requests** — grid with the colour-coded Status column

## Worth adding (these demonstrate the rules, not just the layout)
4. **RA 11261 blocked** — select **Carlo Aquino** (2 months' residency),
   New Document Request → First-Time Jobseeker Certificate.
   The submit button disables and the reason is shown.
5. **RA 11261 allowed** — same steps for **Jose Bautista Jr.** (14 months):
   accepted, FREE OF CHARGE, basis cited.
6. **Fee waiver** — **Maria Reyes** (senior citizen) requesting a Barangay
   Clearance: PHP 0.00, "FREE — Senior Citizen (RA 9994)".
7. **Release blocked on unpaid fee** — try releasing a fee-bearing request
   before recording payment.
8. **Printed certificate** — Requests → View / Print.

## Before / after (if the rubric wants the refactor evidenced)
9. `git checkout ec27edf` → screenshot the old three-tab UI
10. `git checkout main` → screenshot the sidebar UI

Use **Win + Shift + S** and save the PNGs into this folder.
